#include <bits/stdc++.h>
using namespace std;

int n;
int inorder[100000];
int postorder[100000];

int main(void) {
    scanf("%d", &n);
    for(int i=0; i<n; ++i)
        scanf("%d", &inorder[i]);
    
    for(int i=0; i<n; ++i)
        scanf("%d", &postorder[i]);
    
    
}