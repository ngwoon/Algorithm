#include <bits/stdc++.h>
using namespace std;

vector<int> num;

int main(void) {
    int n,m,k;
    scanf("%d %d %d", &n, &m, &k);

    num.resize(n);
    for(int i=0; i<n; ++i)
        scanf("%d", &num[i]);

    
}